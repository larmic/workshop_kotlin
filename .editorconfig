# EditorConfig is awesome: https://EditorConfig.org

# top-most EditorConfig file
root = true

[*]
# Unix-style newlines
end_of_line = lf
charset = utf-8         # keep in sync with checkstyle.xml
indent_style = space
indent_size = 4
max_line_length = 160   # keep in sync with checkstyle.xml